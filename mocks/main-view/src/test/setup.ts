// Vitest setup. Runs once before each test file.
// Registers @testing-library/jest-dom matchers so component tests can
// use .toBeInTheDocument(), .toHaveTextContent(), etc.

import '@testing-library/jest-dom/vitest';
